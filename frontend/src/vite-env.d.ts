/// <reference types="vite/client" />

// Injected by vite.config.ts from build/config.yml's info.version — see the
// comment there. Not a runtime value: Vite substitutes it at build time.
declare const __APP_VERSION__: string
