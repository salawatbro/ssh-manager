# JetBrains
.idea/

# Scratch used by the subagent-driven-development skill
.superpowers/

.task
bin
frontend/dist
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe
# Mobile generated (per-machine; overlays hold absolute paths)
build/ios/xcode/overlay.json
build/ios/xcode/gen/
build/android/overlay.json
build/android/gen/

# Task 1 additions
bin/
frontend/dist/
frontend/node_modules/
*.db
*.db-wal
*.db-shm
.DS_Store

# Bare app binary from `go build .` at repo root (use `task` for real builds)
/sshmgr

# vitest/vite caches land here if a frontend command is run from the repo root
# by mistake (the real dependency tree is frontend/node_modules, ignored above)
/node_modules/

# CI not needed for now (kept locally, not pushed)
.github/workflows/
# Superpowers implementation plans/specs — local working docs only
docs/superpowers/
# Internal version notes — local working docs only
docs/*-notes.md

# /design-sync working files. The durable inputs (.design-sync/config.json,
# NOTES.md, conventions.md, previews/, overrides/) ARE committed; everything
# below is regenerated per run or per machine.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# Converter inputs derived from the frontend build (see .design-sync/NOTES.md)
frontend/.ds-entry.tsx
frontend/.ds-styles.css

# Leftover temp file from an interrupted `task bump` sed/mv chain
# (e.g. build/config.yml.tmp) — see Taskfile.yml's `bump` task.
*.tmp
